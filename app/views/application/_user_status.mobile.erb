<% unless @dialog == true %>

<aside style="float:right;">
<% if current_user %>

  <%= gravatar_image_tag(current_user.email.downcase, :alt => current_user.login, :style => "vertical-align:middle")%>
  <%#= image_tag Gravatar.new(current_user.email.downcase).image_url,, :alt => current_user.login %>
  <%= link_to "#{current_user.login} abmelden", sessions_path, :method => :delete, :class => "ym-button ym-delete" %>
  <%#= link_to "#{current_user.login} (#{current_user.email}) abmelden", destroy_user_session_path, :method => :delete, :class => "ym-button ym-delete" %>
 <%# elsif (controller_name != "sessions") %>
 <% else %>
  <%= link_to "Einloggen", new_sessions_path, :class => "ym-button ym-next" %>
<% end %>
</aside>

<% end %>

