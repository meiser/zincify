<h1>Traversenbuchungen</h1>
<!--
<table>
  <tr>
     <th><%= t('activerecord.attributes.customer.name') %></th>
    <th><%= t('activerecord.attributes.delivery.reference') %></th>
    <th><%= t('activerecord.attributes.delivery.deadline') %></th>
    <th></th>
    <th></th>
  </tr>-->

<div style="margin-bottom: 1em;">
<%= link_to t(".new"), new_booking_path, :class => "ym-button ym-add" %>
</div>

<%= paginate @bookings %>


<% unless @bookings.empty? %>
<nav class="ym-vlist">
<ul>

<% @bookings.group_by(&:delivery).sort.each do |delivery, bookings| %>
  <h3><%= delivery.commission %> (<%= delivery.customer.name %>)</h3>

  <li>
  <% bookings.each do |b| %>
    <%= b.traverse.name %>
  <% end %>
  </li>

<% end %>

</ul>
</nav>
<% end %>

<%#= content_for :col2 do %>
<%# end %>

